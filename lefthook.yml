pre-commit:
  commands:
    check:
      run: npx biome check --staged
    check-links:
      glob: "src/content/docs/**/*.{md,mdx}"
      run: npm run check-links

commit-msg:
  commands:
    lint-commit-msg:
      run: npx --no -- commitlint --edit $1
